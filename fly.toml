# fly.toml app configuration file generated for postulaciones-bot
# Bot de Discord (worker, no web)

app = "postulaciones-bot"
primary_region = "iad"

kill_signal = "SIGINT"
kill_timeout = 5

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
